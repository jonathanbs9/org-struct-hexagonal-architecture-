package main

func annotations() {

	/*
		Package cmd 	 => Entrypoint - package main and function main
		Package internal => Cannot import an internal package
		Package db 		 => SQL schema (schema.sql). To read tables (Example: postgresql)
		Package API (web, http, gRPC) => api.go | routes.go | server.go

	*/
}
